package io.golos.commun4j.abi.writer.preprocessor.model;

import org.junit.Test;

public class Runner {

    @Test
    public void run() {
        // used to trigger the annotation processor from remote debugging (mvnDebug test)
    }
}
